process testZprime2muAnalysis = {
  source = PoolSource {
    untracked vstring fileNames = {"rfio:/castor/cern.ch/user/t/tucker/CMSSW_1_6_7/Zssm1000_fm_RECO.root"}
  }

  untracked PSet maxEvents = { untracked int32 input = -1 }

  service = MessageLogger {
    untracked vstring destinations = {"Zprime.out"}
    untracked PSet Zprime.out = {
      untracked string threshold  = "DEBUG"
      untracked int32 lineLength  = 132
      untracked bool noLineBreaks = true
    }
    untracked vstring debugModules = {"Zprime2muAnalysis"}
  }

#  service = Tracer {}
#  service = SimpleMemoryCheck {}
    
  include "SUSYBSMAnalysis/Zprime2muAnalysis/data/Zprime2muAnalysis.cfi"
#  replace Zprime2muAnalysis.usingAODOnly = true
#  replace Zprime2muAnalysis.useTriggerInfo = false

#  replace Zprime2muAnalysis.doingElectrons = true
#  replace Zprime2muAnalysis.verbosity = 0

  path analysis = { Zprime2muAnalysis }
}
