process testZprime2muAnalysis = {

    untracked PSet maxEvents = {untracked int32 input = -1}

    source = PoolSource {

	untracked vstring fileNames = {

            'file:/uscmst1b_scratch/lpc1/old_scratch/lpctrig/jwerner/MonteCarlo/fastZprimeSSM_mass1000.root'
            #'file:/uscmst1b_scratch/lpc1/old_scratch/lpctrig/jwerner/MonteCarlo/fastSMDrellYan_minMass400.root'
            #'file:/uscmst1b_scratch/lpc1/old_scratch/lpctrig/jwerner/MonteCarlo/fastSMDrellYan_minMass200.root'

	}
    }

    service = MessageLogger {
        untracked vstring destinations = {"cout"}
        untracked PSet cout = {
            untracked string threshold  = "DEBUG"
            untracked int32 lineLength  = 132
            untracked bool noLineBreaks = true
        }
#       untracked vstring debugModules = { "*" }
        untracked vstring debugModules = {"Zprime2muAnalysis"}
    }

    include "SUSYBSMAnalysis/Zprime2muAnalysis/data/Zprime2muAnalysisEl.cfi"

    path p = { Zprime2muAnalysis }
}
