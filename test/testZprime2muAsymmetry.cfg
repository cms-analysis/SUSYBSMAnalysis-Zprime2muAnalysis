process testZprime2muAsymmetry = {
  source = PoolSource {
    #untracked vstring fileNames = { "file:/scratchdisk2/tucker/dy_above400_GEN_SIM.root" }
    #untracked vstring fileNames = { "file:/scratchdisk2/tucker/dy_above400_DIGI_RECO.root" }
    #untracked vstring fileNames = { "file:/scratchdisk2/tucker/Zssm1000_fm_GEN_SIM.root" }
    untracked vstring fileNames = { "file:/scratchdisk2/tucker/Zssm1000_fm_DIGI_RECO.root" }
    #untracked vstring fileNames = { "file:/scratchdisk2/tucker/Zssm3000_fm_GEN_SIM.root" }
    #untracked vstring fileNames = { "file:/scratchdisk2/tucker/Zssm3000_fm_DIGI_RECO.root" }
    
    untracked int32 maxEvents = -1
  }

  service = MessageLogger {
    untracked vstring destinations = { "Zprime.out" }
    untracked vstring categories   = {
      "FwkJob", "FwkReport", "Root_Warning", "Root_NoDictionary"
    }

    untracked PSet Zprime.out = {
      untracked string threshold  = "DEBUG"
      untracked int32 lineLength  = 132
      untracked bool noLineBreaks = true

      untracked PSet FwkReport = { untracked int32 reportEvery = 500 }
      untracked PSet Root_Warning = { untracked int32 limit = 0 }
      untracked PSet Root_NoDictionary = { untracked int32 limit = 0 }
    }

    untracked vstring debugModules = {
      "Zprime2muAsymmetry"
    }
  }

  include "AnalysisExamples/Zprime2muAnalysis/data/Zprime2muAsymmetry.cfi"

  #replace Zprime2muAsymmetry.numFits = 4
  #replace Zprime2muAsymmetry.generatedOnly = true
  #replace Zprime2muAsymmetry.maxParamEvents = 1
  
  #replace Zprime2muAsymmetry.dataSet = "dy_above400"

  #replace Zprime2muAsymmetry.Zssm1000.fitWindow = { 500, 900 }
  
  #replace Zprime2muAsymmetry.Zssm3000.fitWindow = { 1800, 2700 }
  #replace Zprime2muAsymmetry.dataSet = "Zssm3000"
  
  path p = { Zprime2muAsymmetry }
}
