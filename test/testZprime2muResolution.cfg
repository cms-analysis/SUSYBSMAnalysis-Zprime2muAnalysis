process testZprime2muResolution = {
  source = PoolSource {
    untracked vstring fileNames = {"rfio:/castor/cern.ch/user/t//tucker/CMSSW_1_6_7/Zssm1000_fm_RECO.root"}
  }
  untracked PSet maxEvents = {untracked int32 input = -1}
  
  service = MessageLogger {
    untracked vstring destinations = {"Zprime.out"}
    untracked vstring categories   = {
      "FwkJob", "FwkReport", "Root_NoDictionary"
    }
    untracked PSet Zprime.out = {
      untracked string threshold  = "DEBUG"
      untracked int32 lineLength  = 132
      untracked bool noLineBreaks = true
      untracked PSet FwkReport = {untracked int32 reportEvery = 500}
    }
    untracked vstring debugModules = {
      "Zprime2muAnalysis",
      "Zprime2muResolution"
    }
  }
  
  include "SUSYBSMAnalysis/Zprime2muAnalysis/data/Zprime2muResolution.cfi"
#  replace Zprime2muResolution.verbosity = 2
#  replace Zprime2muResolution.dateHistograms = false

#  include "ParticleListDrawer.cff"

#  replace Zprime2muResolution.useTMRforBest = true

  # JMTBAD have a different block of params for real data? 
  #replace Zprime2muResolution.usingAODOnly = true
  #replace Zprime2muResolution.dataSet = "DY40"
  #replace Zprime2muResolution.DY40.upperMassWin = 6000
  #replace Zprime2muResolution.useHistosFromFile = true
  #replace Zprime2muResolution.useTriggerInfo = false

  #replace Zprime2muResolution.dataSet = "DY500"

  #replace Zprime2muResolution.constructGenDil = true

#  replace Zprime2muResolution.doingElectrons = true
#  replace Zprime2muResolution.outputFile = "electron_resolution.ps"
  
  path p = { Zprime2muResolution }
}
