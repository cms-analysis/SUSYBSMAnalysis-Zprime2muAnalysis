process testZprime2muResolution = {

    untracked PSet maxEvents = {untracked int32 input = -1}

    source = PoolSource {
#	untracked vstring fileNames = {"rfio:/castor/cern.ch/user/s/slava/CMSSW_1_3_1/Zssm1000_fm_DIGI_RECO.root"}
	untracked vstring fileNames = {"file:Zssm1000_fm_DIGI_RECO.root"}
    }
  
    # include "FWCore/MessageService/data/MessageLogger.cfi"
    service = MessageLogger {
	untracked vstring destinations = {"Zprime.out"}
	untracked vstring categories   = {
	    "FwkJob", "FwkReport", "Root_NoDictionary"
	}
	untracked PSet Zprime.out = {
	    untracked string threshold  = "DEBUG"
	    untracked int32 lineLength  = 132
	    untracked bool noLineBreaks = true
	    untracked PSet FwkReport = {untracked int32 reportEvery = 100}
	}
	untracked vstring debugModules = {
	    "Zprime2muAnalysis",
	    "Zprime2muResolution"
	}
    }

    include "SUSYBSMAnalysis/Zprime2muAnalysis/data/Zprime2muResolution.cfi"

    path p = {Zprime2muResolution}
}
